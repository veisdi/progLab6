# ==========================================
# Скрипт тестирования генератора ID
# ==========================================

# 1. Показываем текущее состояние коллекции (чтобы видеть начальные ID)
info
show

# 2. Добавляем первого тестового бойца
# Ожидаемый результат: Он должен получить ID = (Максимальный ID в файле + 1)
# Например, если в файле было 2 объекта (ID 1 и 2), этот получит ID 3.
add
TestUser_One
10
20.0
100
true
TestIdCheck
POWER_SWORD
TestChapter
TestWorld

# 3. Показываем коллекцию снова
# Визуально проверьте: у нового бойца должен быть уникальный ID, больший всех предыдущих.
show

# 4. Добавляем второго тестового бойца
# Ожидаемый результат: Его ID должен быть ровно на 1 больше, чем у TestUser_One.
add
TestUser_Two
30
40.0
200
false
SecondIdCheck
CHAIN_AXE
AnotherChapter
AnotherWorld

# 5. Финальный показ
# Проверьте:
# 1. У TestUser_Two ID уникален.
# 2. ID TestUser_Two == ID TestUser_One + 1.
show

# 6. Сохраняем результат в файл, чтобы проверить сохранение ID при следующем запуске
save

# 7. Завершение
exit